document.addEventListener('DOMContentLoaded', function () {
  // Get the reference to the input field inside your new tab
  const terminalInput = document.getElementById('terminalInput');

  // Set the focus to the input field
  terminalInput.focus();
});
